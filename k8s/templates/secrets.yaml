{{- if not .Values.global.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "quantflow.fullname" . }}-secrets
  labels:
    {{- include "quantflow.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgresql-password: {{ required "postgresql.password is required when not using global.existingSecret" .Values.postgresql.password | quote }}
  mongodb-root-password: {{ required "mongodb.rootPassword is required when not using global.existingSecret" .Values.mongodb.rootPassword | quote }}
  minio-access-key: {{ include "quantflow.minioAccessKey" . | trim | quote }}
  minio-secret-key: {{ include "quantflow.minioSecretKey" . | trim | quote }}
  database-url: {{ include "quantflow.databaseUrl" . | trim | quote }}
  mongo-url: {{ include "quantflow.mongoUrl" . | trim | quote }}
  jwt-secret: {{ required "quantflowApi.env.JWT_SECRET is required when not using global.existingSecret" .Values.quantflowApi.env.JWT_SECRET | quote }}
{{- end }}
